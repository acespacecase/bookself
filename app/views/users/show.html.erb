<p id="notice"><%= notice %></p>

<p><%= @user.email %></p>

<% @user.books.all.each do |book| %>
  <p><%= link_to book.title, user_book_path(:user_id => @user.id) %>, by <%= book.author %></p>
<% end %>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
